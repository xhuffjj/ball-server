syntax="proto2"; // 改为 proto2
package playerdata;
message BaseInfo{
    optional int32 playerid=1;
    optional int32 coins=2;
    optional string name=3;
    optional int32 level=4;
    optional int32 last_login_time=5;
    optional int32 skin=6 [default=10]; // proto2 支持默认值
}